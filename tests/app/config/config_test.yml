framework:
    test: ~
    secret: test
    translator: ~
    default_locale:  "en"
    session:
        storage_id: session.storage.mock_file
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    fragments:       ~
    http_method_override: true

jose:
    use_default_json_converter: false
